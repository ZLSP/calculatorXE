package com.zlsp.calcxe.ui.theme.models

import com.zlsp.calcxe.base.Constants

enum class ThemeMode(val id: Int) {
    LIGHT(Constants.THEME_MODE_LIGHT),
    DARK(Constants.THEME_MODE_DARK),
    AMOLED(Constants.THEME_MODE_AMOLED)
}